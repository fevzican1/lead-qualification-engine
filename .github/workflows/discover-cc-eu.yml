name: discover-cc-eu

on:
  schedule:
    - cron: "15,40 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  shard:
    uses: ./.github/workflows/harvest-shard.yml
    with:
      profile: eu
      source: commoncrawl-eu
      shard_count: 50
