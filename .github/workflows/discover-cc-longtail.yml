name: discover-cc-longtail

on:
  schedule:
    - cron: "12,42 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  shard:
    uses: ./.github/workflows/harvest-shard.yml
    with:
      profile: longtail
      source: commoncrawl-longtail
      shard_count: 50
